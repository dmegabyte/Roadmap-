# Профессиональный портфолио на React: Дорожная карта AI-ассистента

## 📘 Обзор проекта

Этот проект представляет собой интерактивную панель, визуализирующую квартальную дорожную карту по созданию сложного, безопасного и масштабируемого AI-ассистента. Он демонстрирует путь от подготовки "сырых" данных до полнофункционального, готового к эксплуатации (production-ready) решения. Это showcase-проект, иллюстрирующий подходы к проектированию сложных систем, управлению данными и созданию качественного пользовательского опыта.

## ⚠️ Дисклеймер: производительность и архитектурный подход

Проект AI-ассистента реализован с использованием RAG (Retrieval-Augmented Generation) как оптимального по стоимости и гибкости решения для корпоративных Q&A-сценариев.

Данный подход обеспечивает баланс между качеством, скоростью и затратами, поскольку:
- позволяет использовать существующие модели (например, GPT-4.1 mini, Gemini 2.5 flash) без обучения с нуля;
- уменьшает вероятность галлюцинаций за счёт фактического контекста;
- требует минимальных затрат на инфраструктуру и масштабирование.

Среднее ожидаемое время отклика (latency) при стандартной нагрузке:
- **~8 секунд** — с включёнными модулями RAG, фильтрации, confident-score и reasoning;
- при оптимизации (fine-tuning или предвычисление контекста) latency может снижаться в пределах **2 секунд**.

Переход к fine-tuning моделей возможен при увеличении бюджета и необходимости повышенной точности. Этот вариант даст:
- рост точности ответов до 95%+;
- сокращение задержек в 3–4 раза;
- увеличение расходов на вычисления и поддержку модели.

Таким образом, текущая версия проекта ориентирована на реалистичный latency ~8 секунд, при этом архитектура позволяет в будущем перейти к более быстрой конфигурации с fine-tuned моделью.

## 🧭 Руководящие принципы

В основе этой дорожной карты лежат фундаментальные принципы, определяющие каждый аспект разработки.

1.  **Ясность превыше всего.** Мы стремимся демистифицировать процесс создания AI. Каждый этап подробно описан с указанием целей. Компоненты, такие как **"Стратегия продукта"** и **"Архитектура диалога"**, делают сложные концепции понятными для всех участников.

2.  **Пользовательский опыт (UX) как основа.** Интерфейс — это не просто обертка, а ключевая часть продукта. Плавные анимации, интуитивная навигация и продуманная визуальная иерархия создают целостный и увлекательный опыт, помогая пользователю сосредоточиться на содержании.

3.  **Целостность контента: Расширять, а не сокращать.** Дорожная карта — это живой документ. Информация добавляется, но не удаляется, чтобы сохранить исторический контекст и логику развития проекта.

4.  **Безупречное качество UI.** Каждый элемент интерфейса, от иконок до отступов, реализован с вниманием к деталям. Использование Tailwind CSS обеспечивает консистентность и адаптивность, а кастомные анимации добавляют "жизни" и интерактивности.

5.  **Логическая информационная архитектура.** Проект строится по принципу "от простого к сложному". Квартальная структура (Q1-Q4) позволяет последовательно раскрывать этапы разработки, где каждый следующий шаг логически вытекает из предыдущего.

6.  **Доступность (Accessibility) — это не опция.** Мы обеспечиваем доступность интерфейса для всех пользователей, используя семантическую разметку и ARIA-атрибуты, чтобы приложением было удобно пользоваться в том числе с помощью вспомогательных технологий.

7.  **Ключевые выводы на каждой странице.** Каждый крупный раздел, будь то квартал или стратегический обзор, содержит четко сформулированные цели и итоговые результаты, позволяя быстро понять суть этапа.

8.  **Компонентная архитектура.** Проект построен на React с использованием модульных, переиспользуемых компонентов. Это упрощает поддержку, тестирование и дальнейшее расширение функциональности.

9.  **Безопасность и этика по умолчанию (Security & Ethics by Design).** Безопасность — не дополнительная функция, а фундамент. Процесс начинается с **обезличивания данных (PII-masking)** и включает **контроль предвзятости (Bias-Monitor)**.

## 🎨 Дизайн-система и руководство по компонентам

Проект придерживается единой визуальной стилистики, создающей ощущение технологичности и профессионализма.

-   **Визуальный стиль:** Темная тема с акцентами на градиентах (от небесно-голубого до фиолетового), фоновые анимации частиц и сеточный паттерн создают глубокую и иммерсивную атмосферу.
-   **Ключевые компоненты:**
    -   `ProductStrategy`: Раскрывающаяся секция для высокоуровневого обзора видения, метрик и персон.
    -   `Roadmap`: Основной интерактивный контейнер, включающий временную шкалу и карточки этапов.
    -   `Timeline`: Навигационный элемент для выбора квартала.
    -   `StageCard`: Основная информационная единица, отображающая детали каждого этапа (описание, таблицы, код).
    -   `CodeSnippet`: Компонент для отображения примеров кода с подсветкой синтаксиса и функцией копирования.
    -   `DialogueFlow`: Визуализация пошаговых процессов и архитектурных потоков.

## 🛠️ Процесс разработки

-   **Структура проекта:**
    -   `src/components`: Все React-компоненты.
    -   `src/data/roadmapData.ts`: **Единый источник правды.** Все текстовое и структурное содержимое дорожной карты хранится здесь.
    -   `src/types.ts`: Определения TypeScript-типов для структуры данных.
-   **Добавление и обновление контента:** Для изменения или добавления информации на дорожной карте достаточно отредактировать массив `roadmapData` в файле `src/data/roadmapData.ts`. Структура данных строго типизирована (см. `types.ts`), что минимизирует ошибки.

## 🚀 Технологический стек и развертывание

-   **Frontend:** React, TypeScript
-   **Styling:** Tailwind CSS
-   **Развертывание:** Проект представляет собой статическое одностраничное приложение (SPA), готовое к развертыванию на любой современной платформе для хостинга статики, такой как Vercel, Netlify, GitHub Pages или Cloudflare Pages.